class Event extends Component {
    constructor( props ) {
        super( props );
    }

    render() {
        return (
            <span>TODO: Remove this before span tag before beginning work in this file.</span>
        );
    }
}

export default Event;
